<div id="signup-page-body" class="container">
  <h1 id="signup-page-title" class="center-block">Please Sign Up!</h1>
    <div id="signup-body">
      <div class="row">
        <div class="col-md-12" >
          
      <%= form_for(@student) do |f| %>
      <div class="form-group" style="margin-left: 0%; margin-right: 30%; margin-top: 10px">
    
      <label for="student_first_name">First Name</label>
      <%= render_sign_up_errors(@first_name_errors) if @first_name_errors%>
      <%= f.text_field :first_name, class: "form-control", autofocus: true, placeholder: "*"%>
      
      <label for="student_last_name">Last Name</label>
      <%= render_sign_up_errors(@last_name_errors) if @last_name_errors%>
      <%= f.text_field :last_name, class: "form-control", autofocus: true, placeholder: "*" %>

      <label for="student_email">Email</label>
      <%= render_sign_up_errors(@email_errors) if @email_errors%>
      <%= f.text_field :email, class: "form-control", autofocus: true, placeholder: "example@concordia.ca" %>

      <label for="student_id">Student ID</label>
      <%= render_sign_up_errors(@student_id_errors) if @student_id_errors%>
      <%= f.text_field :student_id, class: "form-control", autofocus: true, placeholder: "Student ID"%>

      
      <label for="student_department">Department</label>

      <%= f.select :department, Student.departments.each {|d| d}%>


      <label for="student_password">Password</label>
      <%= render_sign_up_errors(@password_errors) if @password_errors%>
      <%= f.password_field :password, class: "form-control", autofocus: true, placeholder: "*" %>

      <label for="student_password_confirmation">Confirm Password</label>
      <%= render_sign_up_errors(@password_confirmation_errors) if @password_confirmation_errors%>
      <%= f.password_field :password_confirmation, class: "form-control", autofocus: true, placeholder: "*" %>

      <%= f.submit "Create Account", class: "btn btn-large btn-primary"%>
      <%= link_to "Home", root_path %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
